import React, { ReactElement } from 'react';
interface Props {
    children?: React.ReactNode;
    name: string;
    tag?: string;
    placeholder?: boolean;
    disable?: boolean;
}
declare const EqualHeightElement: React.MemoExoticComponent<(props: Props) => ReactElement | null>;
export default EqualHeightElement;
